{
  "clientId": "portalclient",
  "clientSecret": "<%= p('cf.uaa.clientsecret')%>",
  "redirectUri": "/callback",
  "logoutredirectUri": "/login",
  "scope": "openid cloud_controller_service_permissions.read cloud_controller.read cloud_controller.write",
  "authUrl": "<%= p('uaa.url')%>/oauth/authorize",
  "checkUrl": "<%= p('uaa.url')%>/check_token",
  "accessUrl": "<%= p('uaa.url')%>/oauth/token",
  "infoUrl": "<%= p('uaa.url')%>/userinfo",
  "logoutUrl": "<%= p('uaa.url')%>/logout.do",
  "userinfoUrl": "/commonapi/v2/user",
  "code": "code",
  "sessionTimeout": "10",
  "monitoring": <%= p('monitoring')%>,
  "quantity": <%= p('quantity')%>,
  "automaticApproval": <%= p('automaticApproval')%>,
  "apiversion": "<%= p('cf_api_version')%>",
  "webadminUri": "http://<%= p('webadmin_ips')%>:<%= link('webadmin-link').p('server.port')%>"
}
